{
  "name": "my-project",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src/ -d dist/",
    "prepublish": "yarn run build"
  },
  "dependencies": {
    "flow-bin": "^0.81.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-flow": "^6.23.0"
  }
}
